sudo: required
services:
    - docker


before_install:
    - docker build -t spanchagnula/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true spanchagnula/docker-react npm run test
    